以下に、他のLLMにスムーズに引き継げるよう、現状を整理した資料を作成しました。

MT4/MT5 Trade Analyzer 開発引き継ぎ資料

1. プロジェクト概要

目的  
MetaTrader 4 / MetaTrader 5 の取引履歴（主にHTMLレポート）をブラウザ上で読み込み、以下の分析を行うツールを作成する。

累積損益（エクイティ）カーブの表示
マジックナンバー別・シンボル別の成績比較
主要指標の計算（純損益、最大ドローダウン、勝率、プロフィットファクターなど）
完全クライアントサイド（オフライン動作、データ外部送信なし）

対象ユーザー  
複数のEAを異なるマジックナンバーで運用している人。ストラテジーテスターの結果も分析したい。

希望機能（優先度順）
標準的なMT4/MT5のHTMLレポート読み込み
マジックナンバーの正確な抽出とフィルタ
エクイティカーブ + 基本指標
期間フィルター
将来的に抽出用EAの作成も検討

実装ファイル: `MT_Trade_Analyzer.html`（単一HTML・依存はChart.jsのCDNのみ）

2. 現在の進捗状況（2026年9月4日 / v0.6 時点）

できていること
HTMLファイルの読み込み自体は成功
取引データの抽出（約2400件程度）
シンボルの抽出
損益の抽出
エクイティカーブの描画
基本的な統計表示（純損益・最大DD・勝率・PF）
マジック・シンボルでのフィルタUI

解決済み（v0.6 / 2026年9月4日）
マジックナンバーとEA名の抽出が動作するようになった。

原因は v0.5 の次の1行だった。

```js
col.comment = (col.profit >= 0 && col.profit + 1 < cells.length) ? col.profit + 1 : cells.length - 1;
```

ここでいう `cells` は**ヘッダー行**のセル配列。MT4の DetailedStatement は
ヘッダーが14セル（Profit = index 13）で終わっており、コメント列はヘッダーに
存在せず**データ行だけ15セル目に付く**。そのため `13 + 1 < 14` が false になり、
「Profitの次の列」は一度も適用されず `col.comment = cells.length - 1 = 13`、
つまり Profit 列そのものを指していた。

これがユーザー報告の症状と完全に一致する。
- Comment列に損益の数値（-5, 273, -227…）が出る
- `extractMagic("-5.00")` は5桁以上の数字を見つけられず `'0'` を返す

つまり「ヘッダー行のセル数でコメント列の境界を判定していた」ことが根本原因で、
v0.4 / v0.5 の修正はこのガードに阻まれて一度も実行されていなかった。

3. ユーザーが使用しているHTMLレポートの特徴（重要）

ファイル名例: DetailedStatement.htm（MT4の詳細レポート）

ヘッダー行
Ticket | Open Time | Type | Size | Item | Price | S/L | T/P | Close Time | Price | Commission | Taxes | Swap | Profit
※ 「Comment」という列名がヘッダーに存在しない

データ行の実際の構造
Profitの次のセルに以下の形式でデータが入っている：

20221121    Assye Lisse EUR/JPY [sl]
20221120    Assye Lisse USD/JPY [tp]
105801      TWICE_Scalping_M5 [sl]

先頭の数字がマジックナンバー
その後ろがEA名 + [sl]や[tp]

balance行（入出金・アーカイブ）も混在しているため、Typeが「balance」の行は除外する必要がある。

4. これまでに試したことと結果

| バージョン | 内容 | 結果 |
|-----------|------|------|
| v0.1〜v0.3 | 一般的なComment列検出 + 正規表現でマジック抽出 | 取引は取れるがマジック全部0 |
| v0.4 | Comment列が見つからない場合は最後の列をCommentにする | 変化なし |
| v0.5 | Profit列の次の列を強制的にCommentとして読む | ユーザー報告「何も変わってない」（ガードに阻まれ未実行だった） |
| v0.6 | ヘッダー長依存の判定を撤去し、行単位のズレ補正＋内容ベース探索に変更 | **解決**（内蔵サンプルで自動照合PASS） |

5. v0.6 のパーサー設計

コメント列の特定（本題）
1. ヘッダーに `Comment` 列があればそれを使う（MT5レポート向け）
2. 無ければ **Profit より右側を行末から内容ベースで探索**する。
   日時・数値・種別語（buy/sell/balance…）・シンボル列を除外し、
   最初に見つかった「文字を含むセル」をコメントとして採用。
   数字のみのコメントを書くEAのために `^#?\d{3,12}$` も許容。
3. それでも空なら `title` 属性を見る（一部ビルド対策）

行ごとの列ズレ補正
MT4/MT5のレポートは行によってセル数が変わる（balance行、colspan、
コメント列の有無）。そこで各行で `buy` `sell` などの**種別語の実位置**を探し、
ヘッダー基準とのズレ量 shift を求めて全列インデックスに加算する。
ズレを検出した場合は診断パネルに `shift dist` として件数が出る。

マジックとEA名の分離
`[sl]` `[tp]` `[pc]` `so:12.3%` `from #123` などMTが自動付加する文字列を
先に除去してから、次の優先順で判定する。
1. ユーザー指定の正規表現（UIの「マジック抽出パターン」）
2. `magic:123` / `magic=123` 形式
3. 先頭の3〜12桁（`20221121 Assye Lisse ...` 形式）
4. `#123` / `[123]` 形式
5. 文中の4〜12桁 → 末尾の3〜12桁
EA名はコメントからマジックと通貨ペアらしいトークン（`EUR/JPY` 等）を
除去した残りを採用する。取れない場合は `(不明)`。

その他の改善
- 文字コード自動判定（meta charset → utf-8 / shift_jis / windows-1252 / 1251）。
  文字化け（U+FFFD）率で採否を決めるため、Shift_JISのレポートでもEA名が壊れない。
- balance / credit / deposit 行は集計から除外し、件数のみ表示。
- 未決済（決済時刻が1つしかない行）は既定で除外。チェックボックスで含められる。
- 数値は空白区切り桁（`1 240.50`）とカンマ小数の両方に対応。
- Chart.js が読めないオフライン環境では内蔵のcanvas描画にフォールバック。
- 診断パネル：使用文字コード、テーブル/行数、ヘッダーの全セル内容と列マップ、
  データ行の生セル内容（先頭6行）、shift分布、実際に採用したコメント列位置、
  マジック/EA名の取得率、警告文。うまくいかない時はここを貼れば原因が分かる。

6. 検証方法

診断パネル内の「サンプルで動作確認」ボタンで、MT4 DetailedStatement の実構造を
再現した内蔵データ（ヘッダー14セル・データ行15セル・balance行・コメント空行・
未決済セクション混在）を読み込める。

コンソールで `selfCheck()` を実行すると期待値と自動照合する。
現在は MT4サンプル6行 + MT5サンプル2行すべて一致して `PASS`。
サンプルの純損益 1306.50 はレポート内の `Closed P/L: 1306.50` と一致する。

7. HTMLレポート経路の限界（2026年9月4日に確認）

v0.6でコメントの取得自体は解決したが、**コメントからマジックを復元する方針そのものに
原理的な限界がある**ことが実データで判明した。

MT4/MT5のHTMLレポートには**マジックナンバーの列が存在しない**。
Comment欄はEAが自由に書く文字列なので、そこからの復元は常に推測になる。

機械的に切り分けられるもの
| 種類 | 判定 | 根拠 |
|---|---|---|
| `145.612` `0.01` | その時のレート/ロット | 小数はマジックになり得ない |
| `20221121` | マジック候補 | 整数 |
| `[sl]` `[tp]` `so:` `from #123` | MTの自動付加 | 仕様で決まっている |
| `EUR/JPY` `eurjpy` | 通貨ペア | 区切りあり、またはItem列と一致 |
| `Assye Lisse` | EA名 | 数字を含まない語 |

原理的に切り分けられないもの
- 整数だが実は値（カウンタ、バー数、UNIX時刻、チケット番号）
- コメントが数字だけのEA（EA名が存在しない）
- `MyEA1234` のように名前と数字が地続き
- 別々のEAが偶然同じ形のコメントを書いている
- 業者側がコメントを書き換えている

ユーザー環境での実例
`20221121 Assye Lisse` と `20221120 Assye Lisse` は**どちらもマジックナンバー**で、
値が違うのは**買いと売りで別マジックを振っている**ため。
一方 `145.6` のような小数は約定時のレート。
つまり「変動するから値」という判定は誤りで、正しい基準は**整数か小数か**。
v0.6の `^#?(\d{3,12})\b` は `145.6` から `145` を拾ってしまうため、
コメントにレートを書くEAでは偽マジックが大量に発生する。

8. 採用した解決策: 抽出用スクリプト（推測をやめる）

`ExportHistoryTSV.mq4` / `ExportHistoryTSV.mq5` を追加。
端末から `OrderMagicNumber()`（MT5は `DEAL_MAGIC`）を直接読んでTSV出力するため、
**推測が一切不要**になる。

手順
1. `<データフォルダ>/MQL4/Scripts/`（MT5は `MQL5/Scripts/`）に置く
2. MetaEditorでコンパイル（F7）
3. ナビゲータの「スクリプト」から任意のチャートにドラッグ
4. `<データフォルダ>/MQL4/Files/` にTSVができる
5. そのTSVを `MT_Trade_Analyzer.html` にドロップ

★MT4は「口座履歴」タブに読み込まれている範囲しか
`OrdersHistoryTotal()` で参照できない。
実行前に口座履歴タブを右クリック →「全履歴」を選んでおくこと。

出力列（タブ区切り。MT4/MT5で完全に同じ）
```
ticket / open time / close time / type / symbol / lots /
open price / close price / magic / commission / swap / profit / net /
comment / close comment
```
- `profit` = HTMLレポートのProfit列と同じ意味（アナライザはこれを使う）
- `net` = profit + commission + swap（+ MT5はfee）
- `close comment` = MT5の決済理由（`sl` `tp` `so` など）。MT4版では常に空
- タブ区切りにしているのはコメントにカンマが入っても壊れないようにするため

★MT5固有の落とし穴（2026年9月8日に修正）

EAが指定したマジックナンバーとコメントは**エントリー側のディールにしか乗らない**。
決済側（`DEAL_ENTRY_OUT`）は

- コメント: 空、またはSL/TP決済なら `sl` `tp` `so` など端末が入れる文字列
- マジック: SL/TP決済だと **0**（SL/TP注文はEAではなくサーバーが出すため）

になる。決済側だけを見るとEA名がまったく取れず、
さらにSL/TP決済分のマジックが全部0になる。

そのため建玉時刻・建値に加えて**マジックとコメントも
同じ `DEAL_POSITION_ID` を持つ `DEAL_ENTRY_IN` 側から引く**。
IN側が空/0のときだけ決済側の値を使う。
決済側のコメントは捨てずに `close comment` 列として別に出す。

MT4は `OrderMagicNumber()` / `OrderComment()` が注文自体に残るのでこの問題は無い。

★アナライザ側の列名の注意

`parseDelimited()` はEA名の列を探すとき `ea` を部分一致でも検索する。
そのため `close reason` や `exit reason` のような列名を付けると
**"reason" の中の "ea" に誤ヒットしてEA名の列と誤認する**。
追加列の名前には `ea` を含めないこと（`close comment` は安全）。

アナライザ側は `parseDelimited()` が `magic` 列を見つけたら
コメント推測を行わず、その値をそのまま使う（v0.6時点で実装済み）。

検証済み
- MetaEditorでコマンドラインコンパイル（`/compile:`）を実行し、
  MT4版・MT5版とも **0 errors, 0 warnings**
- 上記の列構成のTSVを読み込ませて
  マジック `20221121` / `20221120` / `105801` / `880011` すべて正確に取得、
  `(不明)` は magic 0 の手動取引のみ。純損益もHTMLレポートと一致
- MT5形式（15列・決済側コメントが `sl` / `tp` / `so:`）でも
  EA名 `GridMaster` / `PriceGrid` を正しく取得、
  `close comment` 列がEA名の列として誤認されないことを確認
- 実際の口座での実行結果は未検証（この環境では口座に接続していない）

9. 表示・集計の追加（2026年9月8日）

エントリー日時の表示
取引一覧に「エントリー時間」列を追加。HTMLレポート経路では行内の
最初の日時セル、TSV経路では `open time` 列から取る。
未決済行は決済時間を `-` と表示する
（内部の並び替え用 `time` はエントリー時刻で埋めているため、
 そのまま出すと決済済みに見えてしまう）。

集計単位の切り替え
EA名 / EA名+マジック / EA名+シンボル / マジック から選べる。
買いと売りで別マジックを振るEAは「EA名」で1本にまとまり、
「EA名+マジック」で分けられる。

EA名の正規化（トークン方式・2026年9月8日に方式変更）

実データ（4313件・42マジック）で当初の「空白区切りの共通接頭辞」方式が
両方向に破綻していたため、トークン方式に置き換えた。

実際のコメントは変動部分が `_` `:` `,` `#` で名前に**直結**している。

| 実際の表記 | あるべきEA名 | 旧方式の結果 |
|---|---|---|
| `21737:PEACE_0` `26936:PEACE_1` | `PEACE` | 4グループに分裂 |
| `The_HAND:high_1677166200` | `The_HAND:high` | 時刻ごとに8グループへ散乱 |
| `31504866_ロシアンブルー` | `ロシアンブルー` | 番号付きのまま。同一マジックのマンチカンを巻き込み消失 |
| `BlueScalper,173309,S` | `BlueScalper,S` | 番号付きのまま |
| `Premium GBPUSD_0` | `Premium GBPUSD` | `Premium` まで削れて8マジックが1グループに誤統合 |

空白が無い名前では共通接頭辞が取れず最頻値に落ちて任意の1つを採用、
逆に空白がある `Premium GBPUSD_0` では `GBPUSD` まで削る、という
削らなすぎと削りすぎが同時に起きていた。

新方式は2段階。

1. **行単位**: 名前を `\s _ : , ; | / - # @ [ ] ( )` で区切ってトークン化し、
   純粋な数値トークン（連番・UNIX時刻・チケット番号）を落とす。
   区切り文字は保持するので `The_HAND:high` のように元の形が残る。
   英数字が混ざるトークン（`v1.2` `M5` `MT5` `WandamAce2`）は名前の一部として残す。
2. **マジック単位**: グループ内の全名前に共通して現れるトークンだけを残す
   （`Million Dollar MT5` + `Million Dollar Gold` → `Million Dollar`）。
   先頭一致ではなくトークンの有無で見るので、変動部分が先頭でも末尾でも落とせる。

**共通トークンが1つも無い場合は何もしない。**
マジック 20225 のように1つのマジックに `ロシアンブルー` と `マンチカン` が
同居しているケースがあり、最頻値で片方に寄せると片方が消える。
旧方式はこれで実際にマンチカンを消していた。

旧方式にあった「実証済み語幹への前方一致」の後処理は、
行単位の数値除去で不要になったので削除した（誤統合のリスクだけが残るため）。

EA名の表記ゆれの統合（`consolidateEaByMagic`）
MT5では同じEAでもコメントに連番やバージョンが付き
`GridMaster v1.2 #12` `GridMaster v1.2 #47` のように分裂することがある。
マジックが正確なTSV経路なら同じマジック＝同じEAなので、
マジックごとにEA名の共通する先頭部分を取ってラベルにする。

**取引が1件だけのマジックには注意が必要だった。**
比較対象が無いので共通部分＝自分自身になり、接尾辞が残って結局分裂する。
そこで「共通部分が元の名前より短くなった＝接尾辞が実際に変動している」と
確認できた語幹だけを記録し、他の行にも前方一致で適用する二段構えにした。
実証していない語幹では切らないので、`PriceGrid` と `PriceGrid Pro` のような
別EAを誤って統合することはない（検証済み）。

元の表記は `eaRaw` に残し、Comment(生)列でも確認できる。
統合が起きた場合は診断バナーに件数が出る。

10. EA名の手動指定（2026年9月8日）

自動判定はどこまで行っても推測なので、最後は手で決められるようにした。
**キーはマジック番号**。抽出スクリプト経由ならマジックは正確に取れており、
実データでも42件しかないため、コメントのパターン単位より対象が少なくて済む。

UI: 「EA名の対応表」テーブル
マジック / 自動判定 / EA名（手動指定・入力欄）/ 取引数 / 純損益 / 例（生コメント）

- 入力欄を空にすると自動判定に戻る
- 「自動判定」列は上書き後も自動判定の結果を保持して表示する（`t.eaAuto`）
- マジック `0` は「マジックが取れなかった取引」のまとめ行。
  ここに名前を付けると該当する全取引が同じ名前になる
- `input` を作り直すとフォーカスが飛ぶので `change`（確定時）で拾う

保存
`localStorage`（キー `mt_trade_analyzer_ea_map_v1`）に自動保存する。
**ただし開き方によっては使えない。** `data:` URL 経由で描画されると
`SecurityError` になることを確認済み。`file://` で直接開いた場合に
使えるかは環境依存で、今回は実ブラウザでの検証ができていない。

そこで起動時に localStorage の可否を実際に書き込んで判定し、
使えない場合は「JSONで書き出し／読み込み」を使うよう画面に表示する。
JSON書き出しは Blob + `<a download>` で、別PCへの持ち運びにも使える。

`selfCheck()` は手動指定の影響を受けないよう、判定の間だけ
`state.overrides` を空にして実行する
（マジック0に名前を付けるとサンプルの手動取引行が引きずられて誤FAILになる）。

11. 用語・pips・複数ファイル（2026年9月8日）

用語
取引一覧の列を「エントリー時間 / 決済時間」→「エントリー / イグジット」に統一。

pips表示
価格の小数桁からpipの大きさを決める（MTの慣習）。
- 3桁/5桁表示は最後の1桁がpointなので1桁繰り上げる
  USDJPY 135.829(3桁) → pip 0.01 / EURUSD 1.03000(5桁) → pip 0.0001
- それ以外は小数桁そのまま。XAUUSD 1823.89(2桁) → pip 0.01
- 銘柄名からの判定はしない（`-m` などのサフィックスや業者差に弱いため）

`pips = 方向 × (決済値 - 建値) ÷ pipサイズ`。方向は buy が +1、sell が -1。
価格列が無い、または未決済の行は `null` にして集計から除外する（0とはしない）。

「表示単位」セレクタで純損益・最大DD・PF・平均・エクイティカーブを
金額とpipsで切り替える。取引一覧と集計表は両方を常に表示する。
検証: USDJPY 10/5 pips、XAUUSD 64 pips、EURUSD 25 pips、
HTMLレポート経路でも Assye Lisse 80.9 pips / TWICE 0.0 pips（買い-1000+売り+1000）を確認。

複数ファイルの合算
`state.sources[]` に読み込んだファイルを保持し、
結合した全件を `state.trades` として扱う（追加読み込み方式）。

- 同じファイル名を読み直すと差し替え。ソース一覧の「外す」で個別に除去
- **口座が違えばチケット番号は衝突する**ため、`key` に `ソース名:` を前置する
- ソース名は `history_<口座番号>_<日付>.tsv` から口座番号を拾う
- EA名の統合は結合後の全件で1回だけ行う（口座をまたいで同じEAをまとめるため）。
  そのためパーサ内の `consolidateEaByMagic` 呼び出しは削除した
- ソースで絞り込むフィルタを追加
- **口座通貨が違う場合、金額の合計に意味は無い。**
  複数ソース時はその旨をバナーに常時表示し、pips表示を案内する

診断表示の修正
- 手動指定した行が自動判定のように見えていたのを修正（`[手動指定]` と表示）
- 1つのマジックに複数のEA名が残る場合に1つしか表示されなかったのを修正
  （マジック 20225 は ロシアンブルー 48件 / マンチカン 29件 で正しく分かれていた）

12. 期間フィルタ（2026年9月8日）

プリセット + 日付直接指定の併用。

- 全期間
- 直近1 / 3 / 6 / 12ヶ月
- 年別（**データに存在する年だけを列挙する**）
- 期間指定（日付入力欄を直接編集すると自動でこのモードになる）

**基準日は「今日」ではなく「データの最終日」。**
過去のアーカイブを読み込んだときに「直近3ヶ月」が常に0件になるのを避けるため。
基準日は画面に常時表示する。
「今年」を固定文言にせず年を列挙しているのも同じ理由で、
データが2023年で終わっているのに「今年＝2026年」で0件になる事故を防ぐ。

絞り込みは `t.time`（決済日時、未決済はエントリー日時）の日付部分で行う。
`YYYY-MM-DD` の文字列比較で足りるので日付パースはしない。

月をずらす計算は末日で丸める
（`setMonth` をそのまま使うと1月31日の1ヶ月前が3月3日になる）。

ファイルを追加してデータ範囲が変わったときは、
プリセット選択中なら範囲を自動で引き直す。

検証: 2023-01〜2024-03に各月1件のデータで
全期間6件 / 2023年4件 / 2024年2件 / 直近3ヶ月3件 / 直近12ヶ月5件 /
手入力（2023-05-01〜2023-12-31）3件 を確認。

13. エクイティカーブのX軸に日付（2026年9月8日）

`stats()` が `equity` と対になる `dates` を返すようにした。
`equity[0]` は開始点なので最初の取引日を入れて長さを揃える。

目盛りは全点を出すと潰れるので Chart.js の `autoSkip` + `maxTicksLimit: 8` で
自動間引きする。表記は期間の長さで切り替える。

- 400日超 → `2023-04`（年月）
- それ以下 → `02-23`（月日）

目盛りは短縮表示なので、ツールチップでは完全な日付（`2023-01-15`）を出す。
Chart.js が読めないオフライン環境のフォールバック描画にも
5点だけ日付を入れている。

検証: 約900日分180件で目盛り8個・年月表記、
直近3ヶ月に絞ると目盛り7個・月日表記になることを確認。

14. v0.7: 分割決済の集約・列ソート・サイドバー構成（2026年9月8日）

このバージョンから git 管理下に置いた。`ad6967c` が v0.6 のスナップショット。

分割決済・同時刻決済の集約
MT5は1ポジションを分割決済すると決済ディールが複数行になる
（ticket = DEAL_POSITION_ID が同じ）。まとめないとカーブが細かく刻まれ、
取引数も実態より多く出る。

- 「分割決済をまとめる」(既定ON) … 同一ソース・同一チケットで束ねる
- 「同時刻の一括決済もまとめる」(既定OFF) … 同一マジック・同一決済時刻で束ねる

損益とロットは合計、エントリーは最も早い時刻、イグジットは最も遅い時刻。
**pipsは数量加重平均**（単純平均だと小ロットの部分決済が過大評価される）。
2段階まとめるので分割数は引き継ぐ。元データは壊さず結合時に新しい
オブジェクトを作るため、チェックを外せば元の粒度に戻る。
数量加重に必要なロット数を両パーサで取り込むようにした。

列ソート
ヘッダークリックで並べ替え。同じ列を再度押すと昇順/降順が入れ替わる。

- 数字だけの文字列（チケット・マジック）は数値として比較する
  文字列比較のままだと 105801 が 20221120 より後ろに来る
- **空欄は昇順・降順にかかわらず常に末尾へ**
  方向を掛けると降順のとき空欄が先頭に来てしまう
- 文字列は localeCompare(ja)

サイドバー構成
機能を足した順に設定を縦へ積んでいたため、グラフに辿り着くまで
7段のコントロールを越える必要があった。左サイドバー固定＋右メインに再構成。
統計カードを1行に収め、グラフを360→400pxに拡大。
滅多に触らない項目（マジック抽出パターン、EA名の統合トグル）は
対応表の折りたたみ内へ移した。980px以下では1カラムに折り返す。

フィルタの複数選択
プルダウンでは「EA A と B だけ比べる」ができなかったのでチェックリストに変更。
件数表示、絞り込み検索（9件以上のとき）、全選択/解除付き。
絞り込み中の全選択/解除は表示中のものだけを対象にする。
**データを読み直したとき既存の選択は維持し、新しく現れた値は選択済みで登場させる**
（読み込むたびに全部外れると使い物にならない）。

期間UI
プルダウンから セグメントボタン + 前後ナビ(‹ ›) へ。
全期間 / 今週 / 今月 / 今年 / 3ヶ月 / 6ヶ月。
基準日はデータ最終日のまま。日付欄を直接いじるとプリセットの選択が外れる。

比較対象について
有料ツール MT5 Trade Analyzer（note.com/miya76）の無料公開部分と突き合わせた。
向こうにあってこちらに無いのは シャープレシオ / リカバリーファクター のみ
（ユーザー判断で不要とした）。
こちらが優位なのは MT4対応・HTMLレポート直読み・複数口座の合算・
抽出スクリプト・EA名の正規化と手動マッピング・pips表示・診断パネル。

15. スクロール領域の削減（2026年9月9日）

実データ相当（600件・42マジック・23EA・18シンボル）で数えたところ、
スクロール可能な領域が **8個 + ページ本体で9個**あり、入れ子と縦横が混在していた。
取引一覧は縦16,403pxを440pxの窓から覗く状態で、実データ4313件なら12万px。

対処は5点。

1. **メインをタブ化**（集計 / 取引一覧 / 対応表 / 診断）
   4つを縦に並べていたため同時に4つのスクロール領域が存在していた。
2. **テーブルの内側スクロールを廃止**
   `.scroll(max-height:440px)` をやめてページのスクロールに集約。
   **overflow を付けた親があると `thead` の sticky がその親基準になり、
   ページスクロールに追従しなくなる**ため、ラッパー自体を無くした。
   代わりに `table-layout:fixed` で列幅を配分して横スクロールも出さない。
3. **取引一覧をページング**（50件/ページ）
   内側スクロールを外す以上、全件描画は現実的でない。
   絞り込みが変わったら1ページ目へ戻す（`filterSignature` で検知）。
   並べ替えではページを維持する。
4. **サイドバーの入れ子を解消**
   絞り込みリストを `details` にして既定は閉じる。開いたリストは
   高さ制限を付けないので、サイドバー自身のスクロール1本で済む。
   開閉状態は再描画をまたいで保持する（閉じてしまうと使い物にならない）。
5. **横幅の節約**
   日時は分まで表示して秒は `title` へ、Comment(生) は省略表示、
   ソースが1つのときは「ソース」列ごと非表示、
   診断パネルは `pre-wrap` で横スクロールを消した。

結果: 全4タブでスクロール領域ゼロ。42件のマジックリストを開いた状態でもゼロ。
残るのはページ本体のスクロールだけ。

**ハマった点**: CSSで `content:'B8'` のようなエスケープを使ったら
折りたたみの三角が文字化けした。実文字（▸ ▾）を直接書けば確実。

16. 今後の開発方針案

- シャープレシオ、連勝連敗、時間帯別成績
- マジック番号 → EA名の対応表を手で1回作れるUI
  （TSV経路ならマジックは正確なので、あとは
   「このマジックはこのEA」というラベル付けだけ。
   コメントパターン単位より対象が少なく、現実的）